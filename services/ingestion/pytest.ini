[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: requires running Kafka + Redis + PostgreSQL (docker-compose --profile ingestion up -d)
    unit: no external dependencies
addopts = -v --tb=short -m "not integration"

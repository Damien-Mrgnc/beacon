{
  "name": "@beacon/sdk",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@beacon/eslint-config": "workspace:*",
    "@beacon/tsconfig": "workspace:*",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
